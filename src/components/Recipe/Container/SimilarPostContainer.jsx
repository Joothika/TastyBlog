import React from "react";
import { AsideContainer } from "../../../utils/Export";
const SimilarPostContainer = ({ compilations }) => {
  return (
    <>
      <AsideContainer compilation={compilations} />
    </>
  );
};

export { SimilarPostContainer };
